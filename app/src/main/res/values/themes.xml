<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Splash theme (replaced by Compose on first frame) -->
    <style name="Theme.NexusIDE.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0d1117</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.NexusIDE</item>
    </style>

    <style name="Theme.NexusIDE" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
